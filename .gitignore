.DS_Store
.idea
.direnv
result
vendor
.cloud/ressources/.terraform
.cloud/ressources/.terraform.lock.hcl
/.cloud/helm/charts/
coverage.out
dist/
.env
reconciliation

# Isolated local dev stack — Compose file, Dockerfile, gateway config, smoke
# script, and per-developer .env live here. Not part of the shipped product.
local/

# Claude Code per-developer settings (the shared ones live in settings.json).
.claude/settings.local.json
